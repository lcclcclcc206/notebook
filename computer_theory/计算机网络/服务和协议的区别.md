服务是指某一层向他它上一层提供的一组原语（操作）。服务定义了该层准备代表其哟用

户执行哪些操作，大但是它并不涉及如何实现这些操作。服务于与两层之间的接口有关，低层是服务提供者，而上层是服务用户。

协议是一组规则，规定了同上一层对等实体之间所交换的数据包或者报文的格式和含义。对等实体利用协议来实现它们的服务定义，它们可以自由的改变协议，只要不改变呈现它们用户的服务即可。

服务和协议是完全相分离的。

服务设计层与层之间的接口。相反，协议涉及不同机器上两个对等实体之间发送的数据包。