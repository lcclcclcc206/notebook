`DbContext` 通常是一个轻型对象：创建和释放它不涉及数据库操作，而大多数应用程序都可以这样做，而不会对性能产生任何明显的影响。 但是，每个上下文实例确实设置了执行其职责所需的各种内部服务和对象，在高性能方案中，持续执行此操作的开销可能很大。 对于这些情况，EF Core 可以 *共用* 上下文实例：释放上下文时，EF Core 会重置其状态并将其存储在内部池中;下一次请求新实例时，将返回该共用实例，而不是设置新的实例。 上下文池允许你在程序启动时仅支付一次上下文设置成本，而不是连续支付。

请注意，上下文池与数据库连接池正交，后者在数据库驱动程序的较低级别进行管理。